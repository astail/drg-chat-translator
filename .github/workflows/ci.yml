# push と pull request のたびに走るテスト。APIキーは要らない。
#
# リリース（release.yml）はタグを打ったときしか走らないので、普段の変更は
# ここで確かめる。bridge の lint・単体テスト・通し確認と、UE4SS を模した
# スタブでの MOD のロジックの確認（client / host）を行う。

name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 依存を入れる
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt
          sudo apt-get update -q
          sudo apt-get install -y -q lua5.4

      - name: lint
        run: ruff check bridge

      - name: bridge の単体テスト
        run: python -m pytest bridge -q

      - name: bridge の通し確認（--selftest --fake）
        # 同梱する anthropic がこちらの送る引数を受け付けるかも、ここで確かめる
        run: python bridge/drg_bridge.py --selftest --fake --dir "$RUNNER_TEMP/selftest"

      - name: MOD のロジック（UE4SS を模したスタブ）
        run: |
          for role in client host; do
            dir="$RUNNER_TEMP/mock-$role"
            python bridge/drg_bridge.py --fake --no-setup --dir "$dir" > "$RUNNER_TEMP/bridge-$role.log" 2>&1 &
            pid=$!
            sleep 2
            (cd tools && lua5.4 mock_test.lua "$dir" "$role")
            kill "$pid"
          done
